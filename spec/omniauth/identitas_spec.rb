require "spec_helper"

RSpec.describe Omniauth::Identitas do
  it "has a version number" do
    expect(Omniauth::Identitas::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
